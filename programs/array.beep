int main() {
    int a[20]; 
    int b;
    int c; 
    b = 3;
    c = 5;
    a[0] = b + c; 
    cout << a[0];

    a[1] = 100; 
    cout << a[1]; 

    a[2] = 200; 
    cout << a[2];

    a[3] = a[0] * (a[1] + c); 
    cout << a[3];

    return 0;
}
